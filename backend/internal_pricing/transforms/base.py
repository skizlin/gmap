"""Base contract for transforms (subclass or Protocol per your spec)."""


class TransformBase:
    """Placeholder base for named transforms; replace with Protocol if preferred."""

    name: str = "base"
